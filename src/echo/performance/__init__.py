"""Performance ingestion (post metrics collection).

Reserved for a future step. STEP 1 only defines the PerformanceSnapshot
model (echo.models.performance) and its storage table
(echo.storage.repository.EchoRepository.save_performance_snapshot) --
no collector exists yet. Do not implement one here without an explicit
follow-up step; see docs/DEVELOPMENT_RULES.md.
"""
